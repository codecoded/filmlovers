<% set_meta_tags title: "#{presenter.title_with_year} by #{presenter.film.director}" , description: presenter.tagline %>

<% set_meta_tags :og => {
  :title    => film.title,
  :type     => "video.movie",
  :url      => film_url(film),
  :image    => presenter.poster_uri,
  :site_name   =>'filmlovr.com - find, share, discover',
  # :video    => "http://www.youtube.com/embed/#{film.trailer}",
  :description => presenter.overview
} %>

<% set_meta_tags :video =>{
  duration: presenter.duration,
  # director: film.director,
  release_date: film.release_date
} %>

<% set_meta_tags :fb => {
  :admins => '537020179',
  :app_id => Facebook::app_id 
} %>
