<%= render 'film_meta_tags' %>

<% present(film, FilmPresenter) do |presenter| %>
  <div class="full-row inner-hero margin-bottom">
    <div class="row">
      <div class="large-12 small-12 columns">
        <div class="right-fade"></div>
        <div class="left-fade"></div>
        <%= backdrop(film, film.backdrops[0]) if film.has_backdrop? %>
        <!-- <img src="/assets/content/hero-image-man-of-steel.jpg" width="1280" height="720" alt="Man of Steel" /> -->
      </div>
    </div>
  </div>
  <div class="row margin-bottom">
    <div class="large-8 small-12 columns">
      <div class="pod margin-bottom synopsis">
        <h1><%= presenter.title %></h1>
        <h2><%= presenter.year_and_director %></h2>
        <hr />

        <h3>Synopsis</h3>
        <h4><%= film.tagline %></h4>
        <p><%= film.overview %></p>
      </div>
      <div class="margin-bottom">
        <div class="inner-tabs">
          <div class="section-container auto" data-section>
            <%= render partial: 'details', locals: {presenter: presenter} %>
            <%= render partial: 'trailer', locals: {presenter: presenter} if film.has_trailer? %>
            <%= render partial: 'media' if film.has_backdrop? %>
          </div>
        </div>
        <div class="margin-bottom">
          <div class="inner-tabs">
            <div class="section-container auto" data-section>
              <%= render partial: 'cast' %>
              <%= render partial: 'crew' if film.crew? %>
              <%= render partial: 'studios' if film.studios? %>
              <%= render partial: 'locations' if film.locations? %>
            </div>
          </div>
        </div>
      </div>
    </div>
    <div class="large-4 small-12 columns">
      <%= render partial: 'action_panel', locals: {presenter: presenter} if logged_in?%>
      <div class="margin-bottom banner">
        <%= image_tag "content/300x250-banner.jpg", size: '300x250', alt: "Banner" %>
      </div>
      <%if film.genres? %>
        <div class="margin-bottom">
          <h4>Genres</h4>
          <ul class="button-list">
            <% film.genres.each do |genre| %>
              <li><%= genre_link genre %> </li>
            <% end %>
          </ul>
        </div>
      <% end %>
      <%if film.similar? %>
        <div class="margin-bottom">
          <h4>Similar films</h4>
          <ul class="button-list">
            <% presenter.similar_films.each do |similar_film| %>
              <li><%= link_to  similar_film[:title], similar_film[:url] %> </li>
            <% end %>
          </ul>
        </div>
      <% end %>
      <div class="margin-bottom">
        <h4>filmlovers who...</h4>
        <ul class="button-list">
          <li class="watched"><%= presenter.action_count(:watched) %> watched this film</li>
          <li class="loved"><%= presenter.action_count(:loved) %> loved this film</li>
          <li class="owned"><%= presenter.action_count(:owned) %> own this film</li>
        </ul>
      </div>
    </div>
  </div>
<% end %>
<img src="http://www.assoc-amazon.co.uk/e/ir?t=filmlovers-21&l=ur2&o=2" width="1" height="1" border="0" alt="" style="border:none !important; margin:0px !important;" />

        