<% set_meta_tags :title => list.name %>
<% set_meta_tags :description => list.description %>

<% set_meta_tags :og => {
  :title    => list.name,
  :type     => "collection",
  :url      => user_list_url(user, list),
  :image    => (list.films.first.poster('w342') if list.films.first),
  :site_name   =>'filmlovers - find, share, discover',
  :description => list.description
} %>


<% set_meta_tags :fb => {
  :admins => '537020179',
  :app_id => Facebook::app_id
} %>
